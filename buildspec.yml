version: 0.2
phases:
  install:
    commands:
      - echo install
      - [ -d /root/.npm ] && ls /root/.npm || true
      - npm install
      - [ -d /root/.npm ] && ls /root/.npm || true
  pre_build:
    commands:
      - echo pre_build
      - env
      - mkdir artifacts
  build:
    commands:
      - echo build
      - echo Foobar1 > builda.dat
      - echo Foobar2 > artifacts/buildb.dat
      - echo Foobar3 > artifacts/buildc.dat
  post_build:
    commands:
      - echo post_build
cache:
  paths: '/root/.npm'
artifacts:
  files: '**/*'
  base-directory: artifacts
